function setup() {
  createCanvas(500, 500);
  
}

function draw() {
  background(255);
  fill(int(random(50, 250)), 0, 0);
  textSize(30);
  textAlign(CENTER, CENTER);
  text("Profile under construction", width/2, height/2);
}